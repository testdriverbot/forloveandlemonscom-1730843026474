version: 4.0.80
steps:
  - prompt: Assert that the webpage loads successfully.
    commands:
      - command: assert
        expect: The Anniversary Sale is visible on the webpage
  - prompt: >-
      Assert that the company logo is visible.  # Use the content of the file
      from the environment
    commands:
      - command: assert
        expect: for Love & Lemons is visible in the header
