version: 4.0.80
steps:
  - prompt: Click on "Shop All" in the header.
    commands:
      - command: hover-text
        text: Shop All
        description: link in the header
        action: click
  - prompt: Click on "Shop All" in the header.
    commands:
      - command: hover-text
        text: Shop All
        description: link in the header
        action: click
      - command: wait
        timeout: 3000
  - prompt: Click on "Shop All" in the header.
    commands:
      - command: hover-text
        text: Shop All
        description: link in the header
        action: click
      - command: wait
        timeout: 5000
