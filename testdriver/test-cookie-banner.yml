version: 4.0.80
steps:
  - prompt: Click "Accept" on the cookie banner.
    commands:
      - command: hover-text
        text: Accept
        description: button on the cookie banner
        action: click
  - prompt: Click "Accept" on the cookie banner.
    commands:
      - command: hover-text
        text: Accept
        description: button on the cookie banner
        action: click
  - prompt: Click "Accept" on the cookie banner.
    commands:
      - command: hover-text
        text: Accept
        description: button on the cookie banner
        action: click
